# get a list of jobs
type Query {
  job(id: ID!): Job
  jobs: [Job!]
}
# Thw ! means it is mandatory
type Job {
  id: ID!
  title: String!
  company: Company
  description: String
}

type Company {
  id: ID!
  name: String
}
